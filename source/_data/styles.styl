body {
      background:url("/images/background.jpg");
      background-size: cover;
      background-repeat: no-repeat;
      background-attachment: fixed;
      background-position:center center;
}
.content-wrap {
  opacity: 0.9;
}

.sidebar {
  opacity: 0.9;
}

.header-inner {
  background: rgba(255,255,255,0.9);
}

.popup {
  opacity: 0.9;
}

figure.highlight.txt img,
figure.highlight.text img,
figure.highlight.plaintext img
  max-width: 100%
  height: auto
  display: inline-block
  vertical-align: top
  margin: 0